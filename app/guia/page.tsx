// Rota /guia — página de orientações do OSCE. Usa a sidebar global (AppShell).
import GuiaPage from "@/components/guia/GuiaPage";

export const metadata = {
  title: "Guia OSCE — MEDIX",
};

export default function Guia() {
  return <GuiaPage />;
}
